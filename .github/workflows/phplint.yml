name: PHP Linting

on: pull_request

jobs:
  phpcs:

    runs-on: ubuntu-latest

    steps:
        - uses: actions/checkout@v1
        - uses: AricStewart/php-lint@new_composer
          env:
            GH_BOT_TOKEN: ${{ secrets.GH_BOT_TOKEN }}
